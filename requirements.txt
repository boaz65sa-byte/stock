# Core + web API dependencies (this is what Vercel installs).
# Kept lean so the serverless bundle stays well under Vercel's size limit.
fastapi>=0.115.0
uvicorn>=0.30.0
pandas>=2.2.0,<3
numpy>=1.26.0,<3
yfinance>=0.2.40
python-dotenv>=1.0.0
openai>=1.30.0
