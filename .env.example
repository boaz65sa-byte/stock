# Copy this file to .env and fill in the values you have.
# Everything is OPTIONAL - the system works fully with free data (yfinance)
# even if this file is empty.

# --- AI (LLM) analyst agent ---
# If you set an OpenAI key, the "AI Analyst" agent turns on automatically
# and adds a natural-language opinion to the committee.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# --- Optional market-data providers (not required, yfinance is used by default) ---
FINNHUB_API_KEY=
ALPHAVANTAGE_API_KEY=
